{
    "version": "0.2.0",
    "configurations": [
      {
        "args": [
          "--extensionDevelopmentPath=${workspaceFolder}"
        ],
        "name": "Test Extension",
        "outFiles": [
          "${workspaceFolder}/out/**/*.js"
        ],
        "preLaunchTask": "npm: esbuild",
        "request": "launch",
        "type": "extensionHost"
      }
    ]
}
